# {{앱 이름 또는 작업 제목}}

> 이 문서는 **사용자가 구현 전에 자신의 생각을 자유롭게 적는 곳**이다.
> 항목은 가이드일 뿐이며 해당 없는 항목은 비워두거나 삭제해도 무방하다.
> AI 는 본 문서를 읽고 불명확한 부분이 있으면 사용자에게 재질문한 뒤 `.claude/design/` 하위에 설계서를 작성한다.
>
> 본 파일은 **예시** 다. 실제 작성은 `.claude/design/init.md` 에 한다.

---

## 1. 한 줄 요약

> 이 작업이 무엇을 만들거나 바꾸는지 한 문장.

예) "오프라인 메모 앱의 메모 검색 기능을 추가한다."

---

## 2. 배경 / 동기

> 왜 지금 이 작업을 하는가? 어떤 문제·요구가 있었는가?

- {{문제 / 사용자 요구 / 현재 한계}}
- {{기존에 시도한 방법이 있다면}}

---

## 3. 범위 (Scope)

### 3.1 포함

- {{이번 작업에서 만들 / 바꿀 것 1}}
- {{이번 작업에서 만들 / 바꿀 것 2}}

### 3.2 제외 (비목표)

- {{명시적으로 다루지 않을 것 — 다음 단계 / 별 작업}}

---

## 4. 기능 (Features)

> 사용자 관점에서의 동작. 화면 단위가 아니어도 무방.

- {{기능 1 — 예: "메모 목록에서 키워드 입력 시 즉시 필터링"}}
- {{기능 2}}
- {{기능 3 (필요 시 추가)}}

---

## 5. UI / 화면

> 화면이 있는 작업이면 작성. 백엔드 전용이면 생략.

- {{화면 1 — 어떤 컴포넌트·요소·상태 분기가 있는가}}
- {{화면 2}}

스케치·와이어프레임이 있으면 `.claude/reference/` 에 첨부하고 여기서 경로로 참조한다.

---

## 6. 데이터 / 모델

> 어떤 데이터를 다루는가? 형태·출처·저장 위치.

- {{도메인 객체 1 — 필드 개략, 출처(서버 API / 로컬 파일 / 로컬 DB / 사용자 입력)}}
- {{도메인 객체 2}}

---

## 7. 외부 시스템 / 연동

- 백엔드 API: {{있음 / 없음, 있으면 endpoint 또는 명세 위치}}
- 외부 서비스: {{푸시, 분석, 결제 등}}
- 로컬 OS 기능: {{파일 다이얼로그, 알림, 카메라 등}}

> Tauri 의 capability 가 필요한 항목이 보이면 명시한다.

---

## 8. 타깃 / 플랫폼

> 어떤 환경에서 동작해야 하는가.

- [ ] macOS
- [ ] Windows
- [ ] Linux
- [ ] iOS
- [ ] Android

---

## 9. 제약 / 비기능 요구

> 성능·보안·접근성·오프라인·다국어 등.

- {{예: "오프라인 우선 — 네트워크 없을 때도 모든 화면 동작"}}
- {{예: "초기 로드 1초 이내"}}
- {{예: "사용자 인증 정보는 secure store 에만 저장"}}

---

## 10. 일정 / 우선순위

- 마감 또는 시점: {{있다면 작성, 없으면 생략}}
- 우선순위가 다른 항목이 있으면 §4 의 기능 옆에 (High/Med/Low) 표기

---

## 11. 참고 자료

> 사용자가 참고했거나 구현이 참고해야 할 자료.

- {{문서/링크/디자인 시안 경로}}
- 코드/파일 첨부가 필요하면 `.claude/reference/` 하위에 두고 경로 명시
  - 예: `.claude/reference/api-spec.yaml`
  - 예: `.claude/reference/figma-export/`

---

## 12. 불명확하거나 결정이 필요한 항목

> 사용자가 직접 결정하지 못한 항목을 적어두면 AI 가 설계서에서 옵션을 제시한다.

- {{Q1. 예: "검색 결과 정렬을 최신순으로 할지, 관련도순으로 할지?"}}
- {{Q2. 예: "메모 동기화 시점을 자동/수동 중 어디로?"}}

---

> ⚠️ 작성 팁
>
> - 잘 모르는 항목은 비우고 "모르겠음" 이라고 적어도 된다. AI 가 옵션을 제시할 것이다.
> - 기술 결정(예: "Zod 를 쓸지", "TanStack Query 를 쓸지")은 사용자가 정하기 어려우면 비워두고, 설계서 단계에서 AI 권장안을 받는다.
> - 본 템플릿은 가이드일 뿐 — 항목 순서/이름/추가/삭제 자유롭다.
